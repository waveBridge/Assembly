DATA SEGMENT
	FI DW 0000H,0001H,18 DUP (?),'$'
DATA ENDS

CODE SEGMENT
	ASSUME DS:DATA,CS:CODE
START:	MOV AX, DATA
		MOV DS, AX

		MOV CX, 18
		LEA SI, FI
		LEA DI, FI
		LEA BX, FI
		ADD DI, 2
		ADD BX, 4
		
NEXT:	MOV AX, [SI]
		ADD AX, [DI]
		MOV [BX], AX
		
		ADD SI, 2
		ADD DI, 2
		ADD BX, 2
		
		LOOP NEXT
		
		MOV CX, 18
		LEA SI, FI
CHANGE:	MOV AX, [SI]
		ADD AX, 30H
		MOV [SI], AX
		ADD SI, 2
		LOOP CHANGE
		
		MOV AH, 9
		LEA DX, FI
		INT 21H
		
		MOV AH, 4CH
		INT 21H   
		
CODE ENDS
END START